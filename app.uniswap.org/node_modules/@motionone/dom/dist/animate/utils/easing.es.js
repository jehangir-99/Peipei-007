import{isFunction,defaults,isCubicBezier,progress}from"@motionone/utils";import{supports}from"./feature-detection.es.js";const resolution=.015,generateLinearEasingPoints=(easing,duration)=>{let points="";const numPoints=Math.round(duration/.015);for(let i=0;i<numPoints;i++)points+=easing(progress(0,numPoints-1,i))+", ";return points.substring(0,points.length-2)},convertEasing=(easing,duration)=>isFunction(easing)?supports.linearEasing()?`linear(${generateLinearEasingPoints(easing,duration)})`:defaults.easing:isCubicBezier(easing)?cubicBezierAsString(easing):easing,cubicBezierAsString=([a,b,c,d])=>`cubic-bezier(${a}, ${b}, ${c}, ${d})`;export{convertEasing,cubicBezierAsString,generateLinearEasingPoints};