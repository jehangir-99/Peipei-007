import{isNumber,isFunction}from"@motionone/utils";import{getEasingFunction}from"@motionone/animation";function stagger(duration=.1,{start:start=0,from:from=0,easing:easing}={}){return(i,total)=>{const fromIndex=isNumber(from)?from:getFromIndex(from,total),distance=Math.abs(fromIndex-i);let delay=duration*distance;if(easing){const maxDelay=total*duration;delay=getEasingFunction(easing)(delay/maxDelay)*maxDelay}return start+delay}}function getFromIndex(from,total){if("first"===from)return 0;{const lastIndex=total-1;return"last"===from?lastIndex:lastIndex/2}}function resolveOption(option,i,total){return isFunction(option)?option(i,total):option}export{getFromIndex,resolveOption,stagger};