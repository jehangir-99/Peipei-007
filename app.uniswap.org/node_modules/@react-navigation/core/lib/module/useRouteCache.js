import*as React from"react";export const CHILD_STATE=Symbol("CHILD_STATE");export default function useRouteCache(routes){const cache=React.useMemo((()=>({current:new Map})),[]);return"production"===process.env.NODE_ENV?routes:(cache.current=routes.reduce(((acc,route)=>{const previous=cache.current.get(route);if(previous)acc.set(route,previous);else{const{state:state,...proxy}=route;Object.defineProperty(proxy,CHILD_STATE,{enumerable:!1,value:state}),acc.set(route,proxy)}return acc}),new Map),Array.from(cache.current.values()))}