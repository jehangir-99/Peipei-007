"use strict";Object.defineProperty(exports,"__esModule",{value:!0}),global.Buffer=global.Buffer||require("buffer").Buffer;var url=require("url"),Sha256=require("@aws-crypto/sha256-js").Sha256,toHex=require("@aws-sdk/util-hex-encoding").toHex,encrypt=function(key,src,encoding){void 0===encoding&&(encoding="");var hash=new Sha256(key);hash.update(src);var result=hash.digestSync();return"hex"===encoding?toHex(result):result},hash=function(src){var arg=src||"",hash=new Sha256;return hash.update(arg),toHex(hash.digestSync())},canonical_headers=function(headers){return headers&&0!==Object.keys(headers).length?Object.keys(headers).map((function(key){return{key:key.toLowerCase(),value:headers[key]?headers[key].trim().replace(/\s+/g," "):""}})).sort((function(a,b){return a.key<b.key?-1:1})).map((function(item){return item.key+":"+item.value})).join("\n")+"\n":""},signed_headers=function(headers){return Object.keys(headers).map((function(key){return key.toLowerCase()})).sort().join(";")},canonical_request=function(request){var url_info=url.parse(request.url);return[request.method||"/",url_info.path,url_info.query,canonical_headers(request.headers),signed_headers(request.headers),hash(request.body)].join("\n")},parse_service_info=function(request){var parsed=(url.parse(request.url).host.match(/([^.]+)\.(?:([^.]*)\.)?amazonaws\.com$/)||[]).slice(1,3);return"es"===parsed[1]&&(parsed=parsed.reverse()),{service:request.service||parsed[0],region:request.region||parsed[1]}},credential_scope=function(d_str,region,service){return[d_str,region,service,"aws4_request"].join("/")},string_to_sign=function(algorithm,canonical_request,dt_str,scope){return[algorithm,dt_str,scope,hash(canonical_request)].join("\n")},get_signing_key=function(secret_key,d_str,service_info){void 0===secret_key&&(secret_key="");var k_date=encrypt("AWS4"+secret_key,d_str),k_region=encrypt(k_date,service_info.region),k_service=encrypt(k_region,service_info.service);return encrypt(k_service,"aws4_request")},get_signature=function(signing_key,str_to_sign){return encrypt(signing_key,str_to_sign,"hex")},get_authorization_header=function(algorithm,access_key,scope,signed_headers,signature){return void 0===access_key&&(access_key=""),[algorithm+" Credential="+access_key+"/"+scope,"SignedHeaders="+signed_headers,"Signature="+signature].join(", ")},sign=function(request,access_info,service_info){void 0===service_info&&(service_info=null),request.headers=request.headers||{};var dt_str=(new Date).toISOString().replace(/[:-]|\.\d{3}/g,""),d_str=dt_str.substr(0,8),url_info=url.parse(request.url);request.headers.host=url_info.host,request.headers["x-amz-date"]=dt_str,access_info.session_token&&(request.headers["X-Amz-Security-Token"]=access_info.session_token);var request_str=canonical_request(request);service_info=service_info||parse_service_info(request);var scope=credential_scope(d_str,service_info.region,service_info.service),str_to_sign=string_to_sign("AWS4-HMAC-SHA256",request_str,dt_str,scope),signing_key=get_signing_key(access_info.secret_key,d_str,service_info),signature=get_signature(signing_key,str_to_sign),authorization_header=get_authorization_header("AWS4-HMAC-SHA256",access_info.access_key,scope,signed_headers(request.headers),signature);return request.headers.Authorization=authorization_header,request},Signer=function(){function Signer(){}return Signer.sign=sign,Signer}();exports.Signer=Signer,exports.default=Signer;