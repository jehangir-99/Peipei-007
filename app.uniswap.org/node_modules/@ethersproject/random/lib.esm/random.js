"use strict";import{arrayify}from"@ethersproject/bytes";import{Logger}from"@ethersproject/logger";import{version}from"./_version";const logger=new Logger(version);function getGlobal(){if("undefined"!=typeof self)return self;if("undefined"!=typeof window)return window;if("undefined"!=typeof global)return global;throw new Error("unable to locate global object")}const anyGlobal=getGlobal();let crypto=anyGlobal.crypto||anyGlobal.msCrypto;crypto&&crypto.getRandomValues||(logger.warn("WARNING: Missing strong random number source"),crypto={getRandomValues:function(buffer){return logger.throwError("no secure random source avaialble",Logger.errors.UNSUPPORTED_OPERATION,{operation:"crypto.getRandomValues"})}});export function randomBytes(length){(length<=0||length>1024||length%1||length!=length)&&logger.throwArgumentError("invalid length","length",length);const result=new Uint8Array(length);return crypto.getRandomValues(result),arrayify(result)}