import{execTyped}from"../regex.js";const tupleRegex=/^tuple(?<array>(\[(\d*)\])*)$/;export function formatAbiParameter(abiParameter){let type=abiParameter.type;if(tupleRegex.test(abiParameter.type)&&"components"in abiParameter){type="(";const length=abiParameter.components.length;for(let i=0;i<length;i++){type+=formatAbiParameter(abiParameter.components[i]),i<length-1&&(type+=", ")}const result=execTyped(tupleRegex,abiParameter.type);return type+=`)${result?.array??""}`,formatAbiParameter({...abiParameter,type:type})}return"indexed"in abiParameter&&abiParameter.indexed&&(type=`${type} indexed`),abiParameter.name?`${type} ${abiParameter.name}`:type}