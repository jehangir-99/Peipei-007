"use strict";const textDecoder=new TextDecoder,decodeText=bytes=>textDecoder.decode(bytes),textEncoder=new TextEncoder,encodeText=text=>textEncoder.encode(text);function concat(arrs,length){const output=new Uint8Array(length);let offset=0;for(const arr of arrs)output.set(arr,offset),offset+=arr.length;return output}module.exports={decodeText:decodeText,encodeText:encodeText,concat:concat};