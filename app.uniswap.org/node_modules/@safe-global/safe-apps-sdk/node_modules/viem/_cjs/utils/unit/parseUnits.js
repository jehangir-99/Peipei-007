"use strict";function parseUnits(value,decimals){let[integer,fraction="0"]=value.split(".");const negative=integer.startsWith("-");if(negative&&(integer=integer.slice(1)),fraction=fraction.replace(/(0+)$/,""),0===decimals)1===Math.round(Number(`.${fraction}`))&&(integer=`${BigInt(integer)+1n}`),fraction="";else if(fraction.length>decimals){const[left,unit,right]=[fraction.slice(0,decimals-1),fraction.slice(decimals-1,decimals),fraction.slice(decimals)],rounded=Math.round(Number(`${unit}.${right}`));fraction=rounded>9?`${BigInt(left)+BigInt(1)}0`.padStart(left.length+1,"0"):`${left}${rounded}`,fraction.length>decimals&&(fraction=fraction.slice(1),integer=`${BigInt(integer)+1n}`),fraction=fraction.slice(0,decimals)}else fraction=fraction.padEnd(decimals,"0");return BigInt(`${negative?"-":""}${integer}${fraction}`)}Object.defineProperty(exports,"__esModule",{value:!0}),exports.parseUnits=void 0,exports.parseUnits=parseUnits;