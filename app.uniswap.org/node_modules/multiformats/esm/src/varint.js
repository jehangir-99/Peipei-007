import varint from"../vendor/varint.js";export const decode=data=>[varint.decode(data),varint.decode.bytes];export const encodeTo=(int,target,offset=0)=>(varint.encode(int,target,offset),target);export const encodingLength=int=>varint.encodingLength(int);