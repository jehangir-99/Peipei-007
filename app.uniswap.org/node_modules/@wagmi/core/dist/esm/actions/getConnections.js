import{}from"../types/utils.js";import{deepEqual}from"../utils/deepEqual.js";let previousConnections=[];export function getConnections(config){const connections=[...config.state.connections.values()];return"reconnecting"===config.state.status||deepEqual(previousConnections,connections)?previousConnections:(previousConnections=connections,connections)}