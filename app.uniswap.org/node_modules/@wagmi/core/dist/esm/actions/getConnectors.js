import{deepEqual}from"../utils/deepEqual.js";let previousConnectors=[];export function getConnectors(config){const connectors=config.connectors;return deepEqual(previousConnectors,connectors)?previousConnectors:(previousConnectors=connectors,connectors)}