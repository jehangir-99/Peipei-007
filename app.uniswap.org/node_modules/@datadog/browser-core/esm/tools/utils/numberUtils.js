export function performDraw(threshold){return 0!==threshold&&100*Math.random()<=threshold}export function round(num,decimals){return+num.toFixed(decimals)}export function isPercentage(value){return isNumber(value)&&value>=0&&value<=100}export function isNumber(value){return"number"==typeof value}