import{createTransport}from"./createTransport.js";export function custom(provider,config={}){const{key:key="custom",name:name="Custom Provider",retryDelay:retryDelay}=config;return({retryCount:defaultRetryCount})=>createTransport({key:key,name:name,request:provider.request.bind(provider),retryCount:config.retryCount??defaultRetryCount,retryDelay:retryDelay,type:"custom"})}