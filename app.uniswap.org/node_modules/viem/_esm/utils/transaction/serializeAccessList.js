import{InvalidAddressError}from"../../errors/address.js";import{InvalidStorageKeySizeError}from"../../errors/transaction.js";import{isAddress}from"../address/isAddress.js";import{}from"../encoding/toRlp.js";export function serializeAccessList(accessList){if(!accessList||0===accessList.length)return[];const serializedAccessList=[];for(let i=0;i<accessList.length;i++){const{address:address,storageKeys:storageKeys}=accessList[i];for(let j=0;j<storageKeys.length;j++)if(storageKeys[j].length-2!=64)throw new InvalidStorageKeySizeError({storageKey:storageKeys[j]});if(!isAddress(address,{strict:!1}))throw new InvalidAddressError({address:address});serializedAccessList.push([address,storageKeys])}return serializedAccessList}