import{LruMap}from"../lru.js";import{checksumAddress}from"./getAddress.js";const addressRegex=/^0x[a-fA-F0-9]{40}$/;export const isAddressCache=new LruMap(8192);export function isAddress(address,options){const{strict:strict=!0}=options??{};if(isAddressCache.has(address))return isAddressCache.get(address);const result=!(!addressRegex.test(address)||address.toLowerCase()!==address&&strict&&checksumAddress(address)!==address);return isAddressCache.set(address,result),result}