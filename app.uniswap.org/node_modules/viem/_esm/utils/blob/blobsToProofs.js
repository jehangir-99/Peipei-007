import{hexToBytes}from"../encoding/toBytes.js";import{bytesToHex}from"../encoding/toHex.js";export function blobsToProofs(parameters){const{kzg:kzg}=parameters,to=parameters.to??("string"==typeof parameters.blobs[0]?"hex":"bytes"),blobs="string"==typeof parameters.blobs[0]?parameters.blobs.map((x=>hexToBytes(x))):parameters.blobs,commitments="string"==typeof parameters.commitments[0]?parameters.commitments.map((x=>hexToBytes(x))):parameters.commitments,proofs=[];for(let i=0;i<blobs.length;i++){const blob=blobs[i],commitment=commitments[i];proofs.push(Uint8Array.from(kzg.computeBlobKzgProof(blob,commitment)))}return"bytes"===to?proofs:proofs.map((x=>bytesToHex(x)))}