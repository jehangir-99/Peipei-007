import{hexToBytes}from"../encoding/toBytes.js";import{bytesToHex}from"../encoding/toHex.js";export function blobsToCommitments(parameters){const{kzg:kzg}=parameters,to=parameters.to??("string"==typeof parameters.blobs[0]?"hex":"bytes"),blobs="string"==typeof parameters.blobs[0]?parameters.blobs.map((x=>hexToBytes(x))):parameters.blobs,commitments=[];for(const blob of blobs)commitments.push(Uint8Array.from(kzg.blobToKzgCommitment(blob)));return"bytes"===to?commitments:commitments.map((x=>bytesToHex(x)))}