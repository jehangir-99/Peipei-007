import{bytesToHex}from"../encoding/toHex.js";import{sha256}from"../hash/sha256.js";export function commitmentToVersionedHash(parameters){const{commitment:commitment,version:version=1}=parameters,to=parameters.to??("string"==typeof commitment?"hex":"bytes"),versionedHash=sha256(commitment,"bytes");return versionedHash.set([version],0),"bytes"===to?versionedHash:bytesToHex(versionedHash)}