import{SizeExceedsPaddingSizeError}from"../../errors/data.js";export function pad(hexOrBytes,{dir:dir,size:size=32}={}){return"string"==typeof hexOrBytes?padHex(hexOrBytes,{dir:dir,size:size}):padBytes(hexOrBytes,{dir:dir,size:size})}export function padHex(hex_,{dir:dir,size:size=32}={}){if(null===size)return hex_;const hex=hex_.replace("0x","");if(hex.length>2*size)throw new SizeExceedsPaddingSizeError({size:Math.ceil(hex.length/2),targetSize:size,type:"hex"});return`0x${hex["right"===dir?"padEnd":"padStart"](2*size,"0")}`}export function padBytes(bytes,{dir:dir,size:size=32}={}){if(null===size)return bytes;if(bytes.length>size)throw new SizeExceedsPaddingSizeError({size:bytes.length,targetSize:size,type:"bytes"});const paddedBytes=new Uint8Array(size);for(let i=0;i<size;i++){const padEnd="right"===dir;paddedBytes[padEnd?i:size-i-1]=bytes[padEnd?i:bytes.length-i-1]}return paddedBytes}