export function extract(value_,{format:format}){if(!format)return{};const value={};return function extract_(formatted){const keys=Object.keys(formatted);for(const key of keys)key in value_&&(value[key]=value_[key]),formatted[key]&&"object"==typeof formatted[key]&&!Array.isArray(formatted[key])&&extract_(formatted[key])}(format(value_||{})),value}